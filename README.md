A Machine Learning / Pygame / Back-End project involving a paint application built entirely within Python and a Convolutional Neural Network using the MNIST digit dataset.
The CNN model can guess what the user hand-writes in the paint application.
